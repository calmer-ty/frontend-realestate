1. 우선 라이브러리에 api의 데이터의 값을 불러온다, 대신 그 값을 호출 시키지는 않는다.
2. 핸들러를 pages/api에 두고 필요할때 클라이언트에 호출 될 수 있도록 핸들러 함수를 만들어둔다.
3. api를 사용하다보면 여러 api가 필요할 것입니다. 그럴땐 핸들러를 사용하지 않고 데이터= 라이브러리끼리 데이터를 주고 받아 사용하도록 하고 완성된 데이터만 호출하도록 한다.
4. 각각의 api들은 분리하여 관리한다